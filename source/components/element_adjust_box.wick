import { selections } from "@model:flame-editor";
import boxes from "./element_adjust_box_ele.wick";

export default <div>
    <container
    data={selections}
    filter={ a.VALID && a.ACTIVE }>
    <boxes />
</container>
</div>;
