import { selections } from "@model:flame-editor";
import boxes from "./element_hover_box_ele.wick";

export default <container
    data=${selections}
    filter=${m1.VALID && !m1.ACTIVE}>
    <boxes />
</container>;
